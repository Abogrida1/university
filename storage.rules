rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Materials folder - only admins can upload, everyone can read
    match /materials/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.token.role == 'admin';
    }
  }
}
